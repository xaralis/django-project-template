Template storage for django projects
==================================

This repository contains templates for new Django projects. Each template is stored in it's own branch. Master is being the default template usable for most Django projects.